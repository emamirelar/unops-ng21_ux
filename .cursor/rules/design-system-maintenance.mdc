---
description: Tracks design-system customisations that diverge from upstream PrimeNG / PrimeUIX defaults so future upgrades (e.g. PrimeNG 22) can be reconciled safely.
globs: "projects/unops-ux/src/lib/theme/**,projects/unops-ux/src/assets/**,src/app.config.ts,.storybook/preview.ts"
alwaysApply: false
---

# Design-System Maintenance Ledger

Whenever you add, rename, or override anything in the design-system layer, **append an entry to the compatibility log below** so the team can reconcile during the next PrimeNG / PrimeUIX major upgrade.

## Files that form the design-system boundary

| File | Role |
|------|------|
| `projects/unops-ux/src/lib/theme/brand-theme.ts` | Wraps upstream Aura / Lara / Nora presets with `brandOverrides`; exports `brandPresets` map |
| `src/app.config.ts` | Bootstraps PrimeNG with `BrandSoft` via `providePrimeNG` |
| `projects/unops-ux/src/lib/layout/layout.service.ts` | Stores active preset name as string (`'Soft'`) and layout shell state |
| `projects/unops-ux/src/lib/layout/components/app.topbar.ts` | Top bar shell (profile, notifications, etc.) |
| `projects/unops-ux/src/lib/layout/components/app.configurator.ts` | Runtime preset + surface switching via `$t()` |
| `projects/unops-ux/src/assets/tailwind.css` | Duplicates brand primitives as Tailwind `@theme` values; app-level motion keyframes and animation tokens |
| `projects/unops-ux/src/lib/components/ai-card-bg/ai-card-bg.ts` | `ux-ai-card-bg` — animated AI insight card shell (brand-tinted background + blurred SVG blobs); CSS variables for light/dark; `prefers-reduced-motion` disables motion |
| `projects/unops-ux/src/assets/_paginator.scss` | Overrides PrimeUIX paginator `flex-wrap: wrap` with single-row layout, `overflow-x: auto`, and `min-width: 0` so page controls do not wrap to a second line |
| `projects/unops-ux/src/assets/_animations.scss` | App-wide page entrance (`router-outlet + *`), `.card` hover micro-interaction, and `prefers-reduced-motion` override |
| `.storybook/preview.ts` | Uses `BrandSoft` for Storybook PrimeNG provider |

## Rules for making changes

1. **If you rename or remove an upstream identifier** (e.g. the theme names `Aura`, `Lara`, `Nora`, or the `$t()` / `definePreset` API), update **every** file listed above and log the change below.
2. **If you add or change a `brandOverrides` key** (primitive alias, semantic token, or component override), log it below so the upgrade diff can verify it still maps to valid upstream tokens.
3. **If you add a new brand primitive color**, you must also add it to `src/assets/tailwind.css` under `@theme` — and log both here.
4. **If upstream renames a base theme** (e.g. PrimeNG 22 renames `Aura` → something else), use this ledger to find every hardcoded reference that must change:
   - String keys in `brandPresets` (`projects/unops-ux/src/lib/theme/brand-theme.ts`)
   - Default `preset: 'Soft'` in `layout.service.ts`
   - Import paths `@primeuix/themes/aura`, `/lara`, `/nora` in `brand-theme.ts`
   - Storybook stories that import `BrandAura` directly
5. **Never silently absorb an upstream breaking change.** If a PrimeNG update alters token names, theme API, or preset structure, stop and flag it to the user before applying the update.

## Compatibility log

> Add entries in reverse-chronological order. Include the date, what changed, which upstream version it targets, and which files were affected.

| Date | Change | Upstream target | Files affected |
|------|--------|-----------------|----------------|
| 2026-04-30 | **Brand color CSS variable fix:** Moved all brand color definitions from `@theme` to `@theme static` in `tailwind.css`. Tailwind v4 tree-shakes `@theme` variables not referenced by utility classes; this caused `_tags.scss` dark-mode overrides to silently fail because variables like `--color-babygreen-900`, `--color-deepsea-800`, `--color-olive-300/900` were stripped from the CSS output. `@theme static` prevents tree-shaking, ensuring all brand palette values are always emitted as CSS custom properties for use in SCSS and JS. The `--color-*: initial` namespace reset remains in the main `@theme` block. | N/A (Tailwind v4) | `tailwind.css` |
| 2026-04-29 | **Paginator single row:** New `_paginator.scss` (imported from `layout.scss`) sets `.p-paginator` / `.p-paginator-content` to `flex-wrap: nowrap`, horizontal overflow when needed, and thin scrollbar. Opportunity AI `p-paginator` drops `flex-wrap!` from `[pt]` and sets `[pageLinkSize]="3"` for the narrow sidebar. PrimeNG still uses its sliding window of page links (not literal `…` text). | PrimeNG 21 / PrimeUIX paginator styles | `_paginator.scss`, `layout.scss`, `opportunity.ts` |
| 2026-04-29 | **AI card moving background:** Added standalone `AiCardBgComponent` (`ux-ai-card-bg`) with scoped `ux-ai-bg-move` / `ux-ai-fg-move` keyframes, CSS variables for light (`#cce5ff`↔`#ffedf8`, fill `#d0eeff`↔`#e6c7d9`) and dark (`#0d2847`↔`#2d1530`, fill `#003855`↔`#541138`), unique SVG `feGaussianBlur` filter ids, and `prefers-reduced-motion` static fallbacks. Optional Tailwind tokens `--animate-ai-bg-move` / `--animate-ai-fg-move` plus `ai-bg-move` / `ai-fg-move` keyframes in `tailwind.css`. Opportunity AI sidebar now wraps content in `ux-ai-card-bg` (static gradient removed; paginator strip `bg-transparent`). Exported from `@unopsitg/ux` via `lib/components`. | N/A (library + Tailwind v4) | `ai-card-bg.ts`, `lib/components/index.ts`, `public-api.ts`, `tailwind.css`, `opportunity.ts` |
| 2026-04-29 | **Data row hover color:** Added `datatable.row.hoverBackground` / `hoverColor` component override in `brand-theme.ts` (`'{primary.200}'` / `'{surface.950}'` light, `'{primary.800}'` / `'{surface.0}'` dark) so all `p-table` rows use the brand primary hover. Replaced `hover:bg-emphasis` with `hover:bg-primary-200 dark:hover:bg-primary-800` on custom data-row elements in `partners.ts` (list + grid views) to match. Other non-row usages of `hover:bg-emphasis` (buttons, menus, upload zones) left unchanged. | PrimeNG 21 / PrimeUIX Themes 2 | `brand-theme.ts`, `partners.ts` |
| 2026-04-28 | **Mobile sidebar light-mode background:** On mobile (<992px) the sidebar previously showed the dark gradient (`_sidebar_theme_core.scss`) for all themes, making light-mode `--d-*` tokens (dark text) unreadable. Added `:root:not(.app-dark) .layout-sidebar { background: var(--surface-ground); }` in the mobile media query of `_responsive.scss` so light mode uses the page background color, reusing the same tokens as desktop. Dark mode on mobile: primary keeps the gradient; dark/light themes get `.app-dark .layout-sidebar-{dark,light} .layout-sidebar { background: var(--d-sidebar-bg-color); }`. | N/A (layout SCSS layer) | `_responsive.scss` |
| 2026-04-28 | **Active menuitem token indirection:** Replaced hard-coded `--p-primary-600` / `background-color: none` / `--p-primary-500` border in `_sidebar_theme_core.scss` `li.active-menuitem > a` with `--d-menuitem-active-text-color` / `--d-menuitem-active-bg` / `--d-menuitem-active-border`. Added new `--d-menuitem-active-border` token to all three sidebar theme files (`_primary.scss`, `_dark.scss`, `_light.scss`) with per-mode values. Active expanded items now correctly adapt to light/dark mode via the existing `--d-*` system. | N/A (layout SCSS layer) | `_sidebar_theme_core.scss`, `_primary.scss`, `_dark.scss`, `_light.scss` |
| 2026-04-28 | **Dark sidebar theme light-mode fix:** Restructured `_dark.scss` so the base `.layout-sidebar-dark` rule uses body-appropriate tokens for light mode (`--p-text-color`, `--p-text-muted-color`, `--p-primary-700/800`, transparent border) matching the primary theme pattern. Moved white/light-on-dark values into `.app-dark .layout-sidebar-dark` (added `--d-sidebar-border`, `--d-menu-separator-border`, `--d-menuitem-hover-bg`, `--d-menuitem-active-bg`, `--d-menuitem-focus-shadow`). Sidebar stays transparent on desktop; text now adapts to body background in each mode. | N/A (layout SCSS layer) | `_dark.scss` |
| 2026-04-28 | **Sidebar icon color token:** Added `--d-menuitem-icon-color` to all three sidebar theme files. Primary light: `--p-primary-700`, primary dark: `--color-ocean-300`; dark theme (both modes): `--color-ocean-300`; light theme: `--p-primary-700`. Replaced two hard-coded `var(--p-primary-700)` in `_sidebar_vertical.scss` with `var(--d-menuitem-icon-color)`. Compact sidebar state overrides (`--p-primary-500`, `--p-primary-400`, `--color-blue-500`) left intentional. | N/A (layout SCSS layer) | `_primary.scss`, `_dark.scss`, `_light.scss`, `_sidebar_vertical.scss` |
| 2026-04-28 | **Circle border-radius token:** Added `--border-radius-circle: 50%` to `_common.scss` and `--radius-circle: 50%` to `tailwind.css` `@theme`. Replaced hard-coded `border-radius: 50%` in `_topbar.scss` notification badge with `var(--border-radius-circle)`. | N/A (layout SCSS + Tailwind v4) | `_common.scss`, `tailwind.css`, `_topbar.scss` |
| 2026-04-28 | **Tag/chip centralization:** Added global compact `tag` sizing in `brand-theme.ts` (`root.padding`, `label.fontWeight`) and introduced shared tag variants in new `_tags.scss` (`.tag-filter-active`, `.tag-status-*`, `.tag-approval-*`). Imported `_tags.scss` from `layout.scss` and migrated app-level tag usages away from duplicated `::ng-deep`/Tailwind `!important` strings in agreements, opportunity, partners, and dashboard widget files. | PrimeNG 21 / PrimeUIX Themes 2 | `brand-theme.ts`, `projects/unops-ux/src/assets/_tags.scss`, `layout.scss`, `agreements.ts`, `opportunity.ts`, `partner.service.ts`, `statcardwidget.ts`, `socialmediarevenuewidget.ts`, `socialmediauserswidget.ts` |
| 2026-04-28 | **Publishable library:** Extracted `@unopsitg/ux` Angular library under `projects/unops-ux/` (`ng-packagr`), moved `brand-theme`, layout shell components, `LayoutService`, shared `types`, and layout SCSS/Tailwind/logo assets into the library. App consumes via `tsconfig` path `@unopsitg/ux` → `public-api.ts`; `MENU_MODEL` / `SIDEBAR_LOGO` / `TOPBAR_MOBILE_LOGO` injection tokens for host configuration. Added `npm run build:lib`, GitHub Actions workflow on `v*` tags for GitHub Packages. `LandingLayout` remains in `src/app/layout/` (page-specific). | PrimeNG 21 / ng-packagr 21 | `projects/unops-ux/**`, `angular.json`, `tsconfig.json`, `package.json`, `src/app.config.ts`, `src/assets/styles.scss`, `src/app/config/app-menu.ts`, `.github/workflows/publish-unops-ux.yml`, `.storybook/preview.ts` |
| 2026-04-27 | **Motion (supplement):** `tailwind.css` — added `enter-liquid` / `enter-liquid-reveal` keyframes and `--animate-enter-liquid` / `--animate-enter-liquid-reveal` for AI Project Analysis card on `opportunity.ts` (sidebar settle-in + expand panel). Uses `motion-safe:` so reduced-motion users skip heavy blur. | N/A (Tailwind v4) | `tailwind.css`, `opportunity.ts` |
| 2026-04-27 | **Motion layer (CSS-only):** (1) `tailwind.css` — added `@theme` keyframes `fade-in`, `fade-in-up`, `fade-in-down`, `scale-in-subtle`, `slide-in-right` and matching `--animate-*` tokens; added `stagger-1`…`stagger-6` utilities. (2) `_animations.scss` — `router-outlet + *` page enter animation; `.card` hover lift using existing `$sidebarShadow`; `prefers-reduced-motion: reduce` guard. Templates use Tailwind `animate-*` / `stagger-*` on dashboards, feature apps, auth forms, landing/pricing, e-commerce grid, and misc pages. | N/A (Tailwind v4 + layout SCSS) | `tailwind.css`, `layout.scss`, `_animations.scss`, numerous `*.ts` page templates |
| 2026-04-27 | Sidebar theme accessibility fixes: (1) `_dark.scss` — fixed `.app-dark .layout-sidebar-dark` app name from `--p-surface-950` (invisible on same-color bg) to `--p-surface-0`; (2) `_primary.scss` — fixed invalid `var (--p-ocean-100)` syntax (space after `var`), changed dark mode bg from light tints (`blue-200/50`) to dark tints (`blue-800/900`) for proper contrast, fixed app name to `--p-surface-0`; (3) `_light.scss` — fixed bg from `--d-menuitem-focus-shadow` (a box-shadow value) to `--p-surface-0`, set text/name colors for proper contrast on light background. | N/A (layout SCSS layer) | `_dark.scss`, `_primary.scss`, `_light.scss` |
| 2026-04-27 | Added `success: { color: '{green.800}' }` to tag light-mode overrides in `brandOverrides` to fix WCAG AA contrast (green.100 bg / green.700 text was 3.92:1, now green.800 gives 5.46:1). | PrimeNG 21 / PrimeUIX Themes 2 | `brand-theme.ts` |
| 2026-04-23 | Added `--font-size-xs: 0.75rem` to `:root` in `_common.scss` (was referenced but undefined). Both `--font-size-xs` and `--font-size-sm` now documented as aligned to Tailwind's scale, used only by layout SCSS. | N/A (layout layer) | `_common.scss` |
| 2026-04-23 | Initial ledger created. Current state: PrimeNG `^21.0.4`, `@primeuix/themes` `^2.0.0`. Three base themes (Aura, Lara, Nora) wrapped with shared `brandOverrides`. Primitive aliases map brand palette onto upstream Tailwind-style names (`slate→gray`, `zinc→deepsea`, `emerald→green`, etc.). Semantic layer sets `darkblue` as primary and overrides `surface` scales per color scheme. Component overrides for `button` and `tag`. Tailwind CSS mirrors all brand primitives. | PrimeNG 21 / PrimeUIX Themes 2 | `brand-theme.ts`, `tailwind.css`, `app.config.ts`, `layout.service.ts` |
